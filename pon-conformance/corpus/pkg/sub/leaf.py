leaf_name = __name__
leaf_package = __package__
from .. import sib
from .. import sib_x
from .. import init_result
leaf_result = leaf_name + "|" + leaf_package + "|" + sib.x + "|" + sib_x + "|" + init_result
